<%= turbo_stream.prepend "post_comments_#{@post.id}" do %>
  <%= render partial: 'comments/reply', locals: { post: @post, comment: @comment } %>
<% end %>

<%= turbo_stream.replace "comment_form_#{@post.id}" do %>
  <%= render partial: 'comments/form', locals: { p: @post }%>
<% end %>