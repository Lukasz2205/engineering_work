<%= turbo_stream.prepend 'posts-list' do %>
  <%= render partial: 'shared/post', locals: { p: @post } %>
<% end %>

<%= turbo_stream.replace "new_post",
                         partial: "posts/form",
                         locals: { post: Post.new } %>